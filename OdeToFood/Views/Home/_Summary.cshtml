@model Restaurant

<section>
    <h3>@Model.Name</h3>
    <div>
        Cusine: @Model.Cusine
    </div>
    <div>
        <a asp-action="Detail" asp-controller="Home" asp-route-id="@Model.Id">Detail</a>
    </div>
</section>